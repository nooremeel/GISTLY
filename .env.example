MONGODB_URI=
GEMINI_API_KEY=
JWT_SECRET=
PORT=5000
CLIENT_ORIGIN=http://localhost:5173
NODE_ENV=development

# Optional SMTP configuration for email delivery (falls back to Ethereal/console sandbox if omitted)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="Gistly" <no-reply@gistly.app>